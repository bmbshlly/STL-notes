#include<bits/stdc++.h>
#include int long long int
#define for(i,b,a) for(int i = b; i <= a; ++i)
#define cases(w)  int w; cin >> w; while(w--)
#define FIO ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

using namespace std;

int main() {
    FIO
    
    return 0;
}
